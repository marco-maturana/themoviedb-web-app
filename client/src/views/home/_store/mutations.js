export const dialog = (state, dialog) => {
  state.dialog = dialog;
};

export const page = (state, page) => {
  state.page = page;
};

export const query = (state, query) => {
  state.query = query;
};
